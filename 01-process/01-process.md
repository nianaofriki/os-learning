# 进程管理笔记

## fork()
- 创建子进程，调用一次返回两次（父进程返回子进程PID，子进程返回0）
- 注意检查 `pid < 0` 错误

## wait()
- 父进程调用 `wait()` 回收子进程，避免僵尸进程
- 获取退出状态：`WIFEXITED` / `WEXITSTATUS`

## 僵尸进程 & 孤儿进程
- 僵尸：子进程先退出，父进程未回收 → 状态 `Z`
- 孤儿：父进程先退出，子进程被 init 收养（PPID→1）
- 解决：必须调用 `wait()` 或 `waitpid()`