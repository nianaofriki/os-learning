# 进程创建 - fork()

## 1. 理解
- `fork()` 创建一个新的子进程，子进程是父进程的副本（代码、数据、堆栈）。
- 调用一次，返回两次：在父进程中返回子进程的 PID，在子进程中返回 0。

## 2. 踩过的坑
- 一开始没判断 `pid < 0` 的情况，若 fork 失败程序可能崩溃。
- 忘记调用 `wait()` 会导致僵尸进程（后续文件会演示）。

## 3. 学到什么
- 理解写时拷贝（Copy-on-Write）的优化思想。
- 区分 `getpid()` 和 `getppid()`。