# 内存管理笔记

## 动态内存分配
- `malloc()` / `free()`：堆分配，手动释放。
- `mmap()`：内存映射文件，适合大文件或共享内存。

## 页面置换算法
- **FIFO**：先进先出，简单但可能 Belady 异常。
- **LRU**：最近最久未使用，性能好但实现成本高。
- **Clock**：近似 LRU，使用访问位和循环指针。

## 编译运行
- 普通 C 程序：`gcc xxx.c -o xxx && ./xxx`
- mmap 需在 Linux/WSL 下运行。